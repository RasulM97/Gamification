# N7.1 pilot integrity

N7.1 follows `a8cdc6066584358f16ea33ffdc84a8292173ab03`. It addresses the six Founder UAT failures and the related privacy, review, account, and presentation requirements. N8 is out of scope. See [the operation investigation](N7.1-FOUNDER-FAILURES.md) for the original evidence; historical UAT records remain unchanged.

## Economy

The immutable signed ledger is authoritative: net position is the sum of entries, spendable balance is `max(0, net)`, and debt is `max(0, -net)`. Return penalties and reasoned Admin deductions are recorded in full. Future credits reduce debt before producing spendable coins. Redemption cannot create debt. Wallet writes serialize on the user row, including purchases of different rewards. Circulation sums spendable positions rather than net positions.

The existing return schedule remains 5 / 7.5 / 10 coins. The confirmation now displays fractional penalties accurately, along with current balance, debt after return, and spendable balance after return. The change does not rewrite old ledger entries.

## Task access and review

Private employee work is visible to the selected worker, its creator, Admin, and explicitly granted Managers. Private Manager work is visible to the selected Manager and Admin by default. Admin grants viewing and reviewing separately in the task drawer. A reviewer can see the task; viewing alone never grants review authority. No worker can review their own work. Manager work requires Admin or an explicit task reviewer.

Routing to an employee derives the employee audience and enforces capacity. Tasks retain their previous PRIVATE/MANAGEMENT sensitivity even after broadening. Broadening a sensitive task or changing a private recipient requires explicit confirmation in both the UI and API, including handoff, reopening, and reactivation. The audit records actor, old and new audiences, target, and confirmation. Private files and linked history follow task access.

Bootstrap filters tasks, notices, activity, and hidden task references in ledger metadata for the authenticated viewer. Aggregate workload remains available for correct capacity counts without exposing private task content. Demo mode applies the same projection while retaining the full workspace internally for persistence and account switching.

## Accounts and synchronization

Admin → People → Edit changes name, position, role, and active status. Capacity remains in the existing capacity control; fulfillment permission remains separate from the system role. Deactivation retains history and requires cleanup of owned work, pending direct assignments, executor seats, and pending/approved redemptions. The last active Admin cannot be removed. Inactive and activation-pending people cannot sign in, receive assignments, or appear in the dev switcher.

Development switching uses an authenticated, tenant-scoped token exchange and lists all eligible active accounts. It never distributes passwords. Each browser tab binds requests to its current authenticated session. Cross-tab token changes reset identity and state together; old queued requests and responses cannot apply to the new account. Refusals trigger an authoritative refresh and a localized error without changing Test Lab expectations.

Production passwords require at least 8 characters; 12 or more is recommended. The strength meter is guidance. Six- or seven-character passwords are accepted only when both development mode and `CVE_ALLOW_WEAK_DEV_PASSWORDS=true` are explicitly enabled, and weak development passwords require confirmation. Passwords never enter business history or UAT exports.

## Presentation

Task text preserves paragraphs, lists, indentation, and safe links. Task history and cycles sort newest first, with stable ID ordering for equal timestamps. Notifications sound only for new actionable or important notices after interaction; initial history stays silent, bursts are throttled, and each account can mute sound.

Errors use localized semantic messages, remain for ten seconds, pause while hovered or focused, and can be closed above an open drawer. Cancellation history preserves the cancelling actor's name, time, and reason even after profile edits. Dashboard actions have keyboard focus and hover feedback; static metrics remain static.

## Backend and migration

Migration `c71a1d902e64` follows `b72e4d1f8307`. It adds user active status, task viewer/reviewer grants, historical sensitivity, private worker role, and cancellation snapshots. It derives metadata from existing structured history without altering ledger entries, task cycles, authored content, or activity. Historical cancellation attribution is backfilled from the recorded event rather than a user's current name.

New endpoints are `PUT /api/tasks/{id}/access`, `PATCH /api/users/{id}`, `POST /api/dev/switch/{id}`, and `GET /api/auth/password-policy`. Existing routing endpoints accept sensitivity confirmation. New events are `TASK_ACCESS_UPDATED`, `TASK_AUDIENCE_CONFIRMED`, `USER_UPDATED`, `USER_DEACTIVATED`, and `USER_REACTIVATED`.

Backend services are split into task operations, task cycles, rewards, shared operations, access, audience guards, and account lifecycle modules. Authentication routes are separate. Frontend actions, reward transitions, integrity rules, request mapping, and task history are extracted from their larger parent files.

## Automated verification

- TypeScript application and Node configurations: passed.
- Vitest: 542 tests in 24 files passed.
- Backend: 194 tests passed against isolated PostgreSQL, including concurrency, tenant isolation, migration upgrades, and fresh provisioning.
- Playwright regression: 345 tests passed across demo and server modes, including RTL, Test Lab, zero demo API traffic, and 22 focused N7.1 cases.
- Demo and server builds: passed. All ten locale dictionaries have matching keys.
- Compiled production UI with a real API: passed private visibility, delegated review, self-review refusal, signed debt, Admin editing, inactive account refusal, and production dev-tool exclusion; no page errors.
- Development servers updated to `1.7.1-n71`; English and Persian Auto RTL render at `http://localhost:5173` without page errors. Migration preservation hashes match for all 204 pre-existing rows across 15 tables. A local database backup was taken first. The 14 protected local files remain byte-for-byte unchanged.

Production files previously over 500 lines were reduced: backend routes 523 → 465; backend service facade 1,136 → 21; frontend reducer 744 → 560. The remaining reducer contains task transitions; reward transitions, action definitions, and integrity rules are separate. No new production module exceeds 500 lines. Locale data and existing test suites are excluded from the production-code limit.

## Founder UAT (eight checks)

1. Switch between Dana, Marcus, and Ray in two tabs. Confirm identity and task/notification content change together; mark only the current account's notices read.
2. Give a worker a reasoned deduction larger than their balance. Return important work and verify the full 7.5 penalty, debt, zero spendable balance, and later credits offsetting debt.
3. As Admin, edit a clean user's name/title and capacity, deactivate them, verify login/assignment exclusion, then reactivate. Try deactivating a worker with unfinished responsibilities and the sole Admin.
4. Route management work to an employee. Confirm the warning, explicit consent, capacity enforcement, and audit. Repeat after cancellation/reactivation of previously sensitive work.
5. Create private employee and private Manager tasks. Check unrelated accounts cannot see them; grant viewing, then review authority, and revoke each separately. Confirm no self-review.
6. Create normal-priority management-pool work. Each eligible active Manager receives one notice. Verify new actionable sound, mute, no sound on reload, and closable localized errors.
7. Inspect English, Persian, and Hebrew task descriptions, people history, and cycles. Check multiline text, newest-first order, direction Auto/LTR/RTL, and dashboard keyboard focus.
8. Cancel a redemption as its employee, a permitted Manager, and Admin. Rename the cancelling person and confirm the cancellation history still shows the original actor, time, and reason.
