# N6.1 — Actionable attention and clean test workspace

Baseline: `b04425b6733cc68255a41d6da06f8b63c278dcb0`. Version: `1.6.1-n61`.

## Current attention, separate from history

`src/domain/attention.ts` is the single selector used by the attention page,
dashboard and navigation count. It filters visible current tasks:

- Rework: `REJECTED` and owned by the viewer. Management waiting for another
  worker is not a management action. A manager's own rework still counts.
- Assignments: `OPEN`, no owner, specific assignment, and either assigned to
  the viewer or unassigned with a management viewer who can reassign it.

Reassignment, valid ownership, resumed/submitted work, approval or cancellation
removes the corresponding exception automatically. Public claims returned to
the available pool do not count. Specific work handed back without an assignee
does count until management resolves it. No new categories or history table
were introduced. There is no manual dismiss/archive action.

Notifications deliver information and can be read/muted/archived. Activity keeps
business history, including declines and returns. Attention derives only from
current tasks; changing attention never edits either history surface.

## Two distinct Admin controls

**Reset demo data** retains its existing behavior: restore the original seeded
scenario. **Clear test workspace** keeps company and people but removes all
business test state. Its modal explains the deletion and requires the exact,
case-sensitive confirmation `CLEAR`. Cancel, Escape, or invalid confirmation
cannot clear data. The server independently validates the confirmation.

The clean demo helper preserves company, users, roles, positions, capacities,
fulfillment capabilities, upload settings, reward categories, notification mute
preferences and the ID sequence. It clears tasks (including nested submissions,
reviews, cycles, contributions and attachment metadata), rewards (including
executor assignments), redemptions, ledger, notifications and activity. Balances
are derived from the empty ledger, never assigned to synthetic wallet fields.

Reward categories are retained as configuration/taxonomy. Test Lab storage and
locale/direction preferences are separate and untouched. An active UAT session
records one `TEST_WORKSPACE_CLEARED` operation after the authoritative result;
existing events, issues and notes remain exportable. No business Activity event
is recreated by the clear operation. Repeated clear is safe.

## Server boundary and environment safety

`POST /api/admin/test-workspace/clear`, JSON body `{"confirmation":"CLEAR"}`.
The authenticated actor must be ADMIN. All deletion predicates use the actor's
company ID; callers cannot supply another tenant. `CVE_DEV_MODE=true` must be
explicitly configured. The historical implicit default of True is insufficient
for this destructive endpoint; false or omitted configuration refuses it.

Frontend controls are restricted to Admin in Vite development mode. Server
development additionally requires the existing `VITE_CVE_DEV_TOOLS=true` flag.
Demo development is the existing local test environment. Both production builds
hide the new control. No new overlapping environment flags were added.

The reset service uses one PostgreSQL transaction and explicit dependency-ordered,
tenant-scoped deletes. It removes Attachment, Contribution, TaskCycle, Submission,
RewardExecutor, Redemption, LedgerTransaction, Notification, Activity, Task and
Reward rows. It preserves Company, User, CompanySettings and RewardCategory.
Reviews are fields on submission records; transitions are activity/cycle records.
There are no other operational tables in the current schema.

Short-lived table locks prevent concurrent writes from crossing the clear boundary.
They can briefly delay another tenant's writes, but never delete its rows. Any SQL,
serialization or commit failure rolls back the entire clear. Constraints remain
enabled. No TRUNCATE, schema reset, migration execution or reset-tracking table is
used. A cleared company survives application restart without reseeding because
the existing seed guard checks company existence.

## Attachment handling

Real attachments are local files with DB metadata. Before deletion, all paths are
resolved and checked inside the authenticated tenant's directory. No bytes move
before the database commits: rollback or a process crash during the transaction
cannot leave live metadata referencing a removed file. After commit, unreferenced
files move on the same filesystem into a unique `.workspace-clear` quarantine and
are deleted. A cleanup failure is logged; remaining original or quarantined bytes
are unservable because their authenticated attachment metadata no longer exists.
Operators may remove these unreferenced leftovers after checking metadata. Missing
files are tolerated; paths outside the tenant are refused before any deletion.
There is no new durable background cleanup subsystem.

## Verification

- 496 Vitest tests, including 11 focused N6.1 domain cases.
- 157 backend tests, including 15 new real PostgreSQL workspace cases.
- 30 N6.1 browser cases across demo/server, en/fa/ar/he and 768/1440 widths.
- Full N1–N6.1 browser regression: 300 passed (190 demo, 110 server-dev).
  TypeScript project/Node checks, both builds,
  ten-locale parity, zero demo product API calls, and migration-head sanity.
- Real isolated server UI: create a task with an actual attachment, clear via
  typed confirmation, verify HTTP 200 and all zero operational collections,
  preserved configuration, retained UAT note and downloadable operation records.
  No attachment files remain after the successful clear.
- Production previews verify the new control is absent in both build modes.

Destructive verification used `cve_n4_test` for backend tests and a separate
`cve_n61_uat` database for the real UI flow. The founder's current business
workspace and existing UAT evidence were not cleared.

## Founder UAT

1. Decline a specific assignment, check Attention, then reassign it. Attention clears; Activity remains.
2. Return a marketplace claim. It returns to available work without lingering in Attention.
3. In demo, Clear test workspace, check zero work/economy/history, then Reset demo data to restore the seed.
4. In an explicitly enabled server test environment, clear a populated workspace and verify users/roles/capacity remain with zero business data and balances.
5. Keep a Test Lab session active during clear; verify prior evidence remains and exports include the clear operation.

## File audit

The existing 741-line reducer gains only an import, action-union member and
forwarding branch (744 lines). Actual destructive logic is in a focused helper;
no unrelated reducer refactor is justified by this patch. Large locale files are
data dictionaries; seven new keys and two corrected attention descriptions are
applied consistently. Backend services.py and routes.py remain untouched.

| Path | Lines | Action |
| --- | ---: | --- |
| `backend/app/main.py` | 70 | modified |
| `e2e/n1.spec.ts` | 96 | modified |
| `e2e/n5-cases.ts` | 145 | modified |
| `package.json` | 30 | modified |
| `src/App.tsx` | 389 | modified |
| `src/domain/reducer.ts` | 744 | modified |
| `src/features/dashboard/dashboard.selectors.test.ts` | 143 | modified |
| `src/features/dashboard/dashboard.selectors.ts` | 82 | modified |
| `src/features/dashboard/modules/QueueModules.tsx` | 39 | modified |
| `src/features/test-lab/testlab.instrumentation.ts` | 178 | modified |
| `src/features/test-lab/testlab.presentation.ts` | 18 | modified |
| `src/i18n/locales/ar.json` | 894 | modified |
| `src/i18n/locales/en.json` | 894 | modified |
| `src/i18n/locales/fa.json` | 894 | modified |
| `src/i18n/locales/he.json` | 894 | modified |
| `src/i18n/locales/hi.json` | 894 | modified |
| `src/i18n/locales/ja.json` | 894 | modified |
| `src/i18n/locales/ko.json` | 894 | modified |
| `src/i18n/locales/ru.json` | 894 | modified |
| `src/i18n/locales/tr.json` | 894 | modified |
| `src/i18n/locales/zh-CN.json` | 894 | modified |
| `src/runtime.ts` | 35 | modified |
| `src/store.tsx` | 404 | modified |
| `src/views/Admin.tsx` | 193 | modified |
| `src/views/Attention.tsx` | 23 | modified |
| `backend/app/workspace_files.py` | 47 | new |
| `backend/app/workspace_reset.py` | 46 | new |
| `backend/app/workspace_routes.py` | 22 | new |
| `backend/tests/test_n61_workspace.py` | 165 | new |
| `e2e/n61-cases.ts` | 140 | new |
| `e2e/n61-server.spec.ts` | 2 | new |
| `e2e/n61.spec.ts` | 2 | new |
| `src/components/WorkspaceControls.tsx` | 27 | new |
| `src/domain/attention.ts` | 11 | new |
| `src/domain/n61.test.ts` | 60 | new |
| `src/domain/workspace-reset.ts` | 7 | new |

## New localization keys

- `admin.clearTestWorkspace`
- `admin.clearTestWorkspaceDescription`
- `admin.clearTestWorkspaceWarning`
- `admin.clearTestWorkspaceConfirmLabel`
- `admin.clearWorkspace`
- `admin.resetSeedDescription`
- `attention.requiresAction`

Updated existing keys: `attention.emptyHint`, `page.attention.subtitle`.
