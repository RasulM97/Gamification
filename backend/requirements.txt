# CVE backend (M1) — pinned for reproducible builds
fastapi==0.116.1
uvicorn[standard]==0.34.2
sqlalchemy==2.0.52
alembic==1.19.1
psycopg2-binary==2.9.12
pyjwt==2.13.0
bcrypt==4.3.0
python-multipart==0.0.20
pydantic-settings==2.15.0
# dev/test only (kept here so the sandbox and CI use one file):
pytest==9.1.1
httpx==0.28.1
pgserver==0.1.4  # user-space PostgreSQL for tests/dev where no system PG exists
